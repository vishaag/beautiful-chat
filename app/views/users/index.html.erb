<% provide(:title, 'All users') %>
<h1>Talk to someone</h1>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= link_to user.name, user_messages_path(user.id) %>
    </li>
  <% end %>
</ul>
